# Environment
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# ADK
agents/.adk/

# SQLite
*.db
*.sqlite

# macOS
.DS_Store

# Pipeline output — may contain secrets from session state (GITHUB_TOKEN,
# LOCAL_DB_PASSWORD, etc. are written into session state and can end up in
# any state dump or shell-redirected run log — wildcard, not an enumerated
# per-experiment list, so a new experiment/module can't slip through the way
# run_factoryRun.log did).
*_state.json
*.log
pr_export/
