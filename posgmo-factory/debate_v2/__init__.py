"""
Phase 2 — self-contained, LLM-backed debate engine spike.

NOT wired into agents/agent.py, orchestrator.py, or any production code
path. Nothing under debate_v2/ imports from the `agents` package, generates
code, calls GitHub, or touches the live database. Run it standalone via
../run_debate_spike.py.
"""
